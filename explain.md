# Railbound 求解器说明

求解器主体位于 `railbound-worker.js`，通过 Vite 模块 Worker 运行。`railbound-worker-code.js` 只负责生成 Worker URL，不再包含求解器模板字符串。

## 1. 共享规则

Worker 从 `railbound-rules.js` 导入轨道、方向、模拟、可达性、动态特性检测和站台 helper。候选解最终仍由主线程调用同一个 `simulate()` 复核。

## 2. 可达性剪枝

`filterBlanks()` 结合前向和后向可达状态，缩小允许搜索器铺轨的格子集合。零号车场景会保守保留仅前向可达的格子，避免误删零号车路径。

## 3. CSP 路径组合

对规模较小且静态状态可建模的关卡，求解器会：

1. 计算每辆普通火车的最短距离与启发式。
2. 枚举有限数量的单车候选路径。
3. 把多车路径对同一格的端口需求合并为基础轨或 T 轨。
4. 使用束搜索和前向检查缩小组合空间。
5. 用权威模拟器验证组合结果。

全局触发器会改变远端状态，静态 CSP 无法可靠建模，因此这类关卡跳过 CSP。

## 4. DFS 回退

以下情况会进入或继续进入 DFS：

- CSP 不适用或溢出。
- 关卡包含全局触发器。
- 存在零号车，需要联合模拟碰撞与动态状态。
- 存在自变 T 等局部动态状态，CSP 候选不足以作为终局证明。

DFS 会在有限迭代预算内逐步放置基础轨，必要时合并成 T 轨，并同步推进车辆与机关状态。因为存在搜索预算和剪枝，“未找到”不是无解的完备证明。

## 5. 并行策略

主线程按硬件并发度启动多个 Worker，上限为 16。各 Worker 使用不同 seed 探索搜索顺序；主线程保留成本更低且通过模拟验证的候选。

并行度仍需通过真实关卡语料建立基准，未来可改为固定 Worker 池或自适应线程数。

## 6. 消息协议

主线程发送：

```js
{
  type: "solve",
  requestId,
  puzzle,
  seed,
  maxTracksHint
}
```

Worker 返回：

- `progress`：迭代数、剪枝或 CSP 诊断。
- `solution`：一个候选轨道布局。
- `done`：本线程结束、方法和备选结果。

每条返回消息都带原始 `requestId`。主线程会终止旧 Worker，并忽略请求 ID 不匹配的消息。

## 7. 调试原则

- 规则错误先在 `railbound-rules.js` 与 `simulate()` 中复现。
- 搜索不到但手工布局可通过时，再检查 `filterBlanks()`、路径枚举和 DFS 预算。
- Worker 候选必须能由主线程 `simulate()` 复现；不能只相信搜索器内部快速预检。
- 修改消息协议后运行 `npm test`，修改 Worker 导入或构建方式后运行 `npm run build`。

# 移植基线

## 已完成

- 依赖可以通过 `npm ci` 从官方 npm registry 重建。
- Node.js 与 npm 最低版本已写入 `package.json`，并提供 `.nvmrc`。
- 开发和预览服务器默认只监听 `127.0.0.1`。
- `npm test`、`npm run test:puzzles`、`npm run build` 和 `npm run check` 已有统一入口；逐题 Worker 执行器已重建，历史细粒度规则脚本仍待恢复。
- GitHub Actions 在 Node 20、22 上执行锁文件安装、测试和构建。
- Worker 已从模板字符串迁为 Vite 模块 Worker，并直接复用共享规则。
- Puzzle 导入导出已集中到版本化边界层。
- Worker 已具备请求隔离、同步取消、异常处理和卸载清理。
- 构建链已升级至 Vite 8，模块 Worker 已完成测试与生产构建回归。
- 本地 JSON 改为双目录关卡库，读取时通过缩略图选择，不再粘贴原始 JSON。
- 关卡库兼容 `test/*.json` 包装夹具，可载入、仅预览和 helper-only 状态分开展示。
- 主模拟与零号前瞻共用碰撞判断和动态世界状态推进逻辑。
- README、架构、开发和数据格式文档已与当前代码同步。

## 兼容承诺

- 无版本号的历史 Puzzle 按版本 0 导入。
- 同时接受常用蛇形和驼峰字段。
- 旧 T 轨名称会映射为当前名称。
- `railbound-logic.js` 与 `createWorkerBlob` 暂时作为代码级兼容入口保留。

## 有意延后

### Git 历史

当前目录没有可识别的 Git 仓库。尚未自动执行 `git init`，因为移植项目可能需要关联原始仓库或保留上游提交历史。确认来源后再选择：

1. 恢复上游仓库并把当前文件作为迁移提交；或
2. 明确作为新项目初始化仓库并创建基线提交。

### 后续工程化

- 优先恢复缺失的完整规则/求解器/Worker 测试执行器，再进行规则重构。
- 继续拆分 React 主编辑器和内联样式；关卡库已先行独立。
- 继续合并主模拟与零号前瞻剩余的单车移动分支；碰撞和动态状态推进已统一。
- 引入覆盖率和浏览器级 Worker 测试。
- 基于真实关卡语料建立求解性能基准。
